// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "pl.drmstudio.wor.customer"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
