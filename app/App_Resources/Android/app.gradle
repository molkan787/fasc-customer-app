// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "xyz.drmstudio.surjan"
    ndk.abiFilters 'armeabi-v7a','arm64-v8a','x86','x86_64'
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
